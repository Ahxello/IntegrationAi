<Window x:Class="IntegrationAi.Views.InputWindow.InputDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntegrationAi.Views.Windows"
        xmlns:dialogs="clr-namespace:IntegrationAi.ViewModels.Dialogs;assembly=IntegrationAi.ViewModels"
        mc:Ignorable="d"
        Title="InputDialogWindow"
        Left="{Binding Left, Mode=TwoWay}"
        Top="{Binding Top, Mode=TwoWay}"
        Height="{Binding Height, Mode=TwoWay}" 
        Width="{Binding Width, Mode=TwoWay}"
        WindowState="Normal"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance dialogs:InputWindowViewModel}">
    <Grid>
        <TextBox Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Confirm" Command="{Binding ConfirmCommand}" Width="25" Height="15" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="25" Height="15" Margin="5"/>
        </StackPanel>
    </Grid>

</Window>
